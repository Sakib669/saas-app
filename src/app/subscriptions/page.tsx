import { PricingTable } from "@clerk/nextjs";

interface Props {}

const Subscription = ({}: Props) => {
  return (
    <main>
      <PricingTable />
    </main>
  );
};

export default Subscription;
